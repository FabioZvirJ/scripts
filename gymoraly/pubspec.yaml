name: gymoraly
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  # Ícones padrão para iOS e Android
  cupertino_icons: ^1.0.8

  # --- BANCO DE DADOS INTERNO ---
  sqflite: ^2.3.0    # Motor do SQLite para Flutter
  path: ^1.9.0       # Utilitário para encontrar o caminho das pastas no celular
  sqflite_common_ffi: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Se você for usar o logo da imagem, lembre-se de descomentar aqui:
  # assets:
  #   - assets/logo.png